# RESUME — <project name>

**Status:** <running | stopped: <reason> | done>
**Continue with:** `/app-forge <absolute project dir>`

Any fresh context: read `.forge/STATE.json`, `PLAN.md`, `LESSONS.md`, last JOURNAL entries — the disk is the truth; this file only orients you. The skill is `app-forge` (`~/.claude/skills/app-forge/SKILL.md`).

## Facts not derivable from state files
- <e.g. Node 22 required; user wants NO deploy; design skill X was used for UI>

## If stopped — exactly where it stands
- <blocker one-liner + the task ids affected>
- <what was tried (see JOURNAL i<N>–i<M>)>
- <suggested next move>
